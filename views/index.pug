extends layout

block content
  h1= title
  p Welcome to #{title} .....Hello!!!
  button(type="button", class="btn btn-warning", onclick="doBump()") Press Me
  div(id = "bump") Bump the count
  a(href="cake") Cake
    script.
      let count = 0;
      let bump =1;
      function doBump(){
        count+=bump
        bump=bump+1;
        document.getElementById('bump').innerHTML= `Count ${count} with bump ${bump}`
      }
